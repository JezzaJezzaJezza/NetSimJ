#include <cstddef>
#include "BitOps.hpp"

namespace topo {
  class Hypercube {
    const std::size_t n;

    Hypercube(std::size_t size) : n(size) {}
    
    public:

      void getNeighbours() {}
  };
}

